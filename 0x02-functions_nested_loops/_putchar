#include <unistd.h>

/**
* _putchar - writes the c characters to stdout
* @c: The character to print
*
* Return: 1 on success
* on error: -1 is returned and errno is appropriately set.
*/
int _putchar(char c)
{
	return (write(1, &c, 1));
}

